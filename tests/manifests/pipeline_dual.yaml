global:
  output_dir: "tests/output/pipeline_dual"
  default_format: "Text"
tasks:
  - name: "dual_pipeline"
    generator: "ca"
    params:
      seq_length: 30
      extensions:
        rule: 30
    count: 5
    seed: 42
    pipeline: ["normalizer", "token_mapper"]
    output_format: "Text"
