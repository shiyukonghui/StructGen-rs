global:
  output_dir: "tests/output/pipeline_full"
  default_format: "Text"
tasks:
  - name: "full_pipeline"
    generator: "ca"
    params:
      seq_length: 30
      extensions:
        rule: 30
    count: 5
    seed: 42
    pipeline: ["normalizer", "dedup", "diff_encoder", "token_mapper"]
    output_format: "Text"
